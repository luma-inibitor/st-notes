# Verification ledger

The record of every factual claim behind the LTM feedback, its verdict, and where
it was checked. The point of this file is that a claim verified here is settled:
if it comes up again, cite the row rather than re-investigating.

Nothing enters the findings document without a row here.

## What was read

| | |
|---|---|
| Long-Term Memory package | 1.1.6, capability API 1.6 |
| Marinara Engine | 2.4.1 |
| LTM source checkout | `Marinara-Agents` @ `packages/long-term-memory/src/engine/packages/{server,client,shared}/src` |
| Stock LTM UI | `client/src/features/long-term-memory/` — `ReviewQueue.tsx`, `MemoryVault.tsx`, `LtmWorkspace.tsx`, `SourcesWorkspace.tsx`, `LongTermMemoryDetail.tsx`, `MemorySettings.tsx`, `ChatSettings.tsx`, `ActivityView.tsx`, `LastInjectionSummary.tsx` |
| Engine source checkout | `Marinara-Engine` @ `packages/{server,client,shared}/src` |
| Operator tooling (not stock) | `maridiag.cjs` (503 lines), `embprobe.cjs` (178), `migrate.cjs` (1,981), `review-app.cjs` (2,545) |
| Verified | August 2026 |

## Citation convention

**File plus symbol, never line number.** Line numbers rot across releases and a
maintainer reading this later would find a citation pointing somewhere unrelated.
Where an error message exists, quote it verbatim — those are greppable and stable.

## Verdict vocabulary

| Verdict | Meaning |
|---|---|
| `confirmed` | Read in source at the cited symbol, or reproduced with a captured artifact |
| `refuted` | Source says otherwise; the claim as stated is wrong |
| `partial` | Directionally right, overstated or misframed as originally written |
| `attributed` | Behavior belongs to the operator's own tooling, not the stock package |
| `open` | Not yet checked |

---

## Package internals — confirmed

| Claim | Verdict | File | Symbol / anchor |
|---|---|---|---|
| Section text capped at 20,000 chars | `confirmed` | `draft-projector.ts` | inside `mergeSection()`; error `"A projected section exceeds the 20,000-character text limit."` |
| Section capped at 100 contributions | `confirmed` | `draft-projector.ts` | inside `mergeSection()`; error `"A projected section exceeds the 100-contribution limit."` |
| Which sections are additive | `confirmed` | `draft-projector.ts` | `isAdditiveLtmSection()` — timeline always; character except `items`/`progression`; relationship only `history`; world always; tone only `observations`; else `tags.includes("anchor")` or `key === "anchors"` |
| `add_link` is idempotent on duplicates | `confirmed` | `draft-projector.ts` | `linksEqual()` (target + relation + aspect) and `uniqueLinks()`; `changesForMutation` returns `[]` when the link already exists |
| `add_link` disposition is always `merge` | `confirmed` | `draft-projector.ts` | `dispositionForMutation()` |
| Fuzzy dedup exists at all | `confirmed` | `dedup.ts` | `deduplicateUnits()`, `lexicalThreshold = 0.85`, `existingSectionCandidates()` |
| Dedup key never crosses sections or notes | `confirmed` | `dedup.ts` | key is `noteId \0 sectionKey` inside `deduplicateUnits()` |
| Recency cooldown is unreachable | `confirmed` | `ranking.ts`, `retrieval.ts` | `LtmRankCooldown` type, consumed by `reciprocalRankFuse(lanes, { cooldowns })`; sole caller in `retrieval.ts` invokes `reciprocalRankFuse(lanes)` with no options, so `cooldowns` is always `undefined` |
| Provenance labels stripped before injection | `confirmed` | `chunking.ts` | `cleanLongTermMemoryChunkText()`, `LEGACY_LABEL_SUFFIX_PATTERN`, `INLINE_EVIDENCE_LABEL_PATTERN` |
| Timeline grounding is a whole-draft invariant | `confirmed` | `reconciliation.ts` | `preflight()`; errors `"Timeline event {id} must link to draft source {sourceNoteId}."` and `"Long-term memory {id} must link to a timeline event grounded in the same source."` |
| Dependency auto-inclusion covers undecided only, never rejected | `confirmed` | `reconciliation.ts` | `filterAutoApplyDependencies()` plus `autoIncludedMutationIds` accumulation in `applyInner()`; draws `add_link` and its `create_note` from `draft.mutations`, and rejected mutations are removed from the draft before that runs |
| Partial accept returns the draft to `pending` with only unselected mutations | `confirmed` | `reconciliation.ts` | `applyInner()` → `updateDraftStatusUnlocked(id, partial ? "pending" : "accepted", { applyState: partial ? "not_started" : "complete", mutations: partial ? <only skipped> : … })` |
| Low-risk auto-apply gate | `confirmed` | `reconciliation.ts` | `lowRisk()` — requires `risk === "low"`, excludes scene-prefixed notes and notes carrying `conflicts` |
| Seven mutation kinds | `confirmed` | shared schema | `create_note`, `append_section`, `update_section`, `add_link`, `set_keywords`, `set_status`, `set_subjects` |
| Extraction fingerprint is gated | `confirmed` | `source-processing.ts` | `canMarkCurrent()` — fingerprint written only on `success`, deterministic `partial_success` with no error diagnostics, or `no_suggestions_created` with zero drops |
| Freshness derived at read time, never stored | `confirmed` | `draft-review.ts` | `draftFreshness()` |
| Rejection records are structured | `confirmed` | `routes.ts`, extraction | `/rejected-suggestions`; each carries reason code, message, source snippet, and a `recovery` object naming note type, note id, section key and status |
| Embedding host is hard-wired local | `confirmed` | `capability-embedding.service.ts` | returns `localEmbed(...)` unconditionally; space id `local:Xenova/all-MiniLM-L6-v2:q8:mean:normalized:v1` |
| Budget computes per-candidate explain data | `confirmed` | `budget.ts` | typed `rejectionReason` from `budget`, `lower_rank`, `score_threshold`, `duplicate_text`, `missing_chunk`, plus lane scores, raw lane scores, cooldown penalty, tier, estimated tokens |
| Injection is verified against the real prompt | `confirmed` | `generation-injection.ts` | `recordGenerationLongTermMemoryDispatch()` checks `isLongTermMemoryPromptPresent(messages, receipt.artifact.content)` before recording |

## Engine internals — confirmed

| Claim | Verdict | File | Symbol / anchor |
|---|---|---|---|
| A prompt-assembling preset can void agent injection, receipt included | `confirmed` | `packages/server/src/routes/generate.routes.ts` | `presetOwnsAgentPlacement`; guard reads `if (handledByPresetSection \|\| !presetOwnsAgentPlacement)` — when the branch is skipped there is no injection and no `longTermMemoryRecallReceipt` assignment |
| Automated summary entries hard-capped | `confirmed` | `packages/shared/src/utils/chat-summary-entries.ts` | `MAX_AUTOMATED_CHAT_SUMMARY_ENTRIES = 200` |
| Day summaries inside a consolidated week are skipped | `confirmed` | `packages/server/src/routes/generate/conversation-history-runtime.ts` | `dayToWeek` map; `if (args.dayToWeek.has(date)) continue;` |

## Measurements

Numbers produced by analysis rather than read from source. The distinction
matters and is stated wherever these are cited.

| Measurement | Method | Result |
|---|---|---|
| Cost of a full dedup pass | Line-for-line port of `tokenize()`, `jaccardSimilarity()` and `deduplicateUnits()`, run over 1,142 claims against 161 notes × 5 sections (3.2 MB stored text). **Not the shipped function under test.** | 123 ms to index stored sections, 10 ms to compare all units, **133 ms total** — no embeddings, no model calls, no network |
| Jaccard score of an exact-duplicate claim (~11 tokens) against the whole stored section, as the section grows | same port | 226 chars → 0.478 · 568 chars → 0.186 · 2 KB → 0.060 · 5 KB → 0.040 · 20 KB → 0.040. Threshold is 0.85, so it never fires |
| `tokenize()` truncation effect | same port | slices at 500 tokens, so a 20 KB section compares identically to a 5 KB one |
| Restatement in a real saturated section | Dedupe pass at 0.7 shingle similarity | 35,805 → 4,327 chars, i.e. **88% restatement** |
| Low-durability content in a real identity note | Manual audit | ~10% removable with no information loss |
| Semantic duplicates lexical dedup passed through | Embedding sweep over corpus bullets, cosine threshold ≈0.88 | ~35 genuine same-fact-different-words pairs |
| Corpus shape | Tokenizer count over the source file | 97 entries, 53,933 tokens, max 1,405 tokens / 60 key details, 17 over threshold; after cleanup 96 entries, max 776 tokens, 45,646 tokens |
| Extraction rejection breakdown | `GET /rejected-suggestions` after a full run | 200 rejected: 132 `untrusted_subject`, 29 `invalid_format` (`evidence: Required`), 28 `invalid_format` (`dimensions`/`dimensionChanges: null`), 16 `unsupported_bucket`, 7 `missing_source_evidence` |
| Summary context reclaimed by the migration | Token counts before and after | 71,635 → 10,050 |

## Corrections to earlier drafts

Recorded here only so the same wrong conclusion is not reached a third time. These
are settled; the findings document states the correct version without narrating
the history.

| Earlier claim | Correct position |
|---|---|
| The preset `agent_data` marker section is not required — deleting it left injection working | The marker is **conditionally** required. The shipped default preset does not take the `presetOwnsAgentPlacement` branch, so injection survives without it. A preset that assembles the whole prompt does take that branch, and there the missing marker voids injection entirely. Both observations were accurate; the general claim drawn from the first was not. |
| There is no fuzzy subject matching | Fuzzy matching exists. `subject-identity.ts` matches against a trusted catalogue and records the basis (`exact_name`, `unique_alias`, `spelling_variation`, `trait_or_qualified_alias`, …). The real gap is that **nothing merges notes that have already split** — identity repair reported 0 merge candidates for the pair it was needed for. |
| Deduplication is exact-match only | Fuzzy dedup exists (`dedup.ts`, Jaccard ≥ 0.85, both directions, keyed per note + section). The real gap is that the incoming↔stored comparison is structurally unable to fire on a non-trivial section. |
| The Chat Settings injection readout is a flat count | It is a `<details>` element; expanding it lists each recalled memory with token counts and links through to the vault. The real gap is the absence of a negative state and a timestamp. |

## Stock review UI — what actually exists

Checked in `client/src/features/long-term-memory/`. Several earlier claims were
overstated; the corrected position is what the findings document states.

| Claim as originally written | Verdict | What is actually true | Where |
|---|---|---|---|
| Rejections are invisible, reachable only through the API | `refuted` | A collapsed `<details>` panel titled "Suggestions that weren't saved" sits in the review workbench, fed by `/rejected-suggestions`. Per item it shows the proposed content, a mapped "Why it wasn't saved" label, "What was expected" built from the `recovery` object, and a "Recommended fix" — and offers two actions: **Recover manually**, which hands off to the Vault prefilled and deletes the suggestion on save, and **Delete**. The real gap is that it is collapsed by default and scoped to the selected source. | `ReviewQueue.tsx` → `data-ltm-rejected-suggestions`, `recoveryLabel()`, `onRecoverCandidate`; `MemoryVault.tsx` recovery handoff |
| A rejection has no action attached, so it is inert even when shown | `refuted` | The verb exists (Recover manually). The finding is that it was never found or understood, which is discoverability and labelling, not absence. | as above |
| No bulk action at all | `refuted` | Per-mutation checkboxes, a select-all with indeterminate state, and a sticky batch bar offering "Accept eligible (n)", "Skip selected (n)" and "Clear", plus per-row accept/skip buttons. The Vault has more: select-all-visible, bulk status, bulk modes, bulk delete with `retractExtracted`. | `ReviewQueue.tsx` → `SelectionCheckbox`, `runBatch()`, `data-ltm-review-batch-actions`; `MemoryVault.tsx` |
| — new finding replacing it | `confirmed` | Selection is wiped by an effect on any change of chat, source or selected draft, so a batch can never span drafts or sources even though `runBatch` already groups its requests by draft. Bulk action is real but confined to one draft at a time. | `ReviewQueue.tsx` → `selectedIds` reset effect vs `runBatch()` grouping |
| No filter, no search, no sort | `partial` | True of the review queue, which hard-codes its query to `/drafts/review?status=pending` with no controls at all. False of the Vault, which has search with clear, a status filter, scope and context-target selectors, `<details>` groups per note type, and a "n hidden by filters" line. Vault sort is alphabetical by title and not user-changeable. | `ReviewQueue.tsx` query; `MemoryVault.tsx` search/`statusFilter` |
| Review decisions do not persist, so a second sitting starts over | `partial` | Decisions are applied immediately — each click POSTs to `/drafts/:id/accept` or `/skip` — so no applied work is ever lost. What does not survive a reload or navigation is in-progress **selection and edits**, held only in React state. The onboarding flag and admin secret are the sole `localStorage` users in the feature. | `ReviewQueue.tsx` → `runBatch()`, `selectedIds`/`editedById` reset effect |
| Edit-before-accept is completely undiscoverable | `partial` | It exists and works: an editor inside the expanded mutation panel, sent as `editedMutations` on accept, covering note title, section text and per-section importance. But it carries no Edit label, button or icon, sits two levels deep past evidence and diagnostics, and returns nothing for `add_link`, `set_keywords`, `set_status` and `set_subjects` — genuinely uneditable for 4 of the 7 mutation kinds. | `ReviewQueue.tsx` → `MutationEditor`, `editedMutations` assembly |
| Conflicts force a binary replace-or-keep choice | `refuted`, and the truth is worse | The review queue never reads `mutation.note.conflicts` at all — the string "conflict" does not occur in the file — so a `create_note` carrying conflicts presents the reviewer with no conflict information and no options whatsoever. The only rendering is read-only in the Vault, showing field, resolution status and `conflict.proposed` while never displaying `conflict.existing`, so there is no diff anywhere. | `ReviewQueue.tsx` (zero occurrences); `MemoryVault.tsx` conflict block |
| The workspace is eight steps down through Chat Settings | `refuted` | Navigation is a persistent four-tab rail (Vault / Review / Sources / Settings) with a pending-review badge, defaulting to Vault. From an open chat it is roughly 3 steps to the Vault and 4 to the review queue, or 2 and 3 via the direct button in the in-chat settings slot. | `LongTermMemoryNavigation.tsx`; `LongTermMemoryDetail.tsx`; `ChatSettings.tsx` |
| Review iterates drafts when the story is organised by subject | `partial` | The hierarchy is source → draft → target → mutation, so `targets[]` **is** consumed. But a target renders as an unlabeled wrapper element with no header, so the grouping is imperceptible, and the axis is hard-coded with no pivot or group-by control. | `ReviewQueue.tsx` target mapping and `fallbackTargets` |
| Drop reasons are presented as undifferentiated blocked items | `partial` | All 11 extraction drop reasons have distinct labels **and** distinct recommended fixes. All 8 draft block reason codes render as a raw humanized enum name plus message, with no explanation or fix. Neither is aggregated or grouped by cause; rejected items are a flat list. | `ReviewQueue.tsx` → `rejectionReasonLabels`, `rejectionRecommendedLabels`, block reason rendering |
| Mutations reference targets by opaque unclickable id | `partial` | Ids are broadly resolved to titles, and rows offer "Open memory" and "Open source"; Vault link pills are clickable. Two real gaps remain: the projection preview prints `change.before`/`change.after` raw without humanizing, and an `add_link` row never surfaces its link target at all — so the referenced timeline event is invisible rather than merely opaque, and there is no editor for that kind either. | `ReviewQueue.tsx` → `humanizeReviewText()`, target title resolution, change preview |
| No cap pressure indicator or pruning affordance | `confirmed` | No keyword count and no enforcement of the 30-keyword cap in the editor, no character counters anywhere, and `contributions` is never rendered in any component. The only cap behavior is **silent truncation** of section text at 20,000 characters in the editor. | `MemoryVault.tsx` → `TokenEditor`; `ReviewQueue.tsx` → `maxLength`/`boundedTrim` |
| Nothing shows how often a memory has been used | `confirmed` | No cumulative recall counter exists in client, server or schema. The last-injection summary shows only the most recent recall; the activity view shows only the latest recall workflow. | `LastInjectionSummary.tsx`; `ActivityView.tsx`; schema |
| No keyboard navigation, no progress tracking | `partial` | No accept/reject/next shortcuts; the only key handling is roving-tab arrows and Escape/Enter in shared controls. Counts of sources, pending mutations, ready and blocked are rendered, which is queue depth rather than "x of y reviewed". | `LtmWorkspace.tsx`; `ReviewQueue.tsx` review summary |

**Follow-up raised by this pass:** the activity view renders the latest recall
workflow with per-candidate scores and rejection reasons, which bears directly on
the claim that budget explain data never reaches the interface. Whether that
surface is gated on a debug flag is being checked separately.

## Attribution — stock package vs operator tooling

The operator built a separate review workbench and migration orchestrator. Several
claims turned out to describe those rather than the package.

| Claim | Verdict | Where it actually lives |
|---|---|---|
| The 20,000-character section error omits the note id, which is in scope at the throw site | `confirmed` as stock, **but the "in scope" half is wrong** | `draft-projector.ts` → `mergeSection()` throws it with code `projection_limit_exceeded`. `mergeSection` receives neither note id nor section key — they exist one frame up in `applyMutation` as `current.id` and `mutation.sectionKey` and are not threaded down. So this is a structural omission, not a message-formatting oversight, and the fix is slightly larger than a string change. The operator's tooling only pattern-matches the stock string to attach its own explanation. |
| The codebase never names the object in a projection error | `refuted` | It is inconsistent, not uniformly anonymous. `evidence-unit-validation.ts` emits `` `Generated target note id '${noteId}' exceeds the long-term memory storage contract.` `` and `identity-repair.ts` → `blockers()` emits key-qualified messages such as `` `${key} exceeds the 20,000-character section limit.` `` |
| Incoming↔stored dedup compares a claim against the whole section as one blob | `confirmed` | `dedup.ts` → `existingSectionCandidates()` pushes exactly one candidate per section, `{ text: section.text.trim(), tokens: tokenize(text) }`, never splitting on lines. In-batch comparison via `seenInBatch` is unit-to-unit and does work. Threshold `lexicalThreshold = 0.85`; key `noteId \0 sectionKey`. |
| `tokenize()` truncates at 500 tokens | `confirmed`, with an additional detail | `utils.ts` → `tokenize()` does `.slice(0, 500)` **and** filters out every token shorter than `minLength = 4`, so short words never participate in similarity at all. The 500 cap is a prefix slice, so late section content is invisible. |
| The operator's tooling duplicates the stock dedup | `refuted` — it is materially different | `review-app.cjs` → `markDupes()`, `shingles()`, `jac()`, `lines()`, `dedupeSection()`: line-level rather than blob-level, 4-word shingles rather than length-≥4 unigrams, thresholds 0.45 and 0.7, no token cap, and it keeps the longest of each cluster instead of dropping. This is why its stored-vs-incoming detection found duplicates the stock path cannot. |
| Semantic dedup is missing because the embedding binding is broken | `refuted` — it is missing **by construction** | The write path (`evidence-unit-extraction.ts` → `deduplicateUnits` → `draft-projector.ts`) never calls an embedding function; there are no embedding references in `dedup.ts`, `draft-projector.ts`, `source-extraction.ts` or `source-processing.ts`. Embeddings appear only in the index and retrieval half — `embedding-adapter.ts`, `rebuild.ts`, `retrieval.ts` → `embeddingsAvailable`. A dead embedding binding degrades recall quality; it has no bearing on dedup. |
| The stock review export produces an HTML file that breaks under an Android `content://` URI | `attributed` — not stock at all | The package has exactly two downloadable exports, neither HTML and neither the review queue: `GET /debug-log/export` (a `.jsonl` attachment, consumed by `ActivityView.tsx`) and `GET /backup/export` (a `.json` backup, consumed by `MemorySettings.tsx`). No `text/html` response exists anywhere. The stock review queue is a live React view. The HTML artifact is `migrate.cjs` → `buildReviewHtml()`, written to `ltm-review.html`; the script's own comment says it serves over localhost to avoid `content://` URIs. **This finding must be dropped.** |
| There is no affordance for keyword curation | `partial` | Keywords can be hand-curated: a token editor in the note editor plus `PATCH /notes/:id`. What does not exist is any ranking, cap-aware trim control, or server-side auto-prune — nothing that acts when the 30-keyword cap is hit, at which point it is a hard projection failure. `identity-repair.ts` → `blockers()` does report "Combined keywords exceed the 30-keyword note limit." as a merge blocker. The blind auto-prune-and-retry is the operator's own (`review-app.cjs`). |
| None of the steps a real migration requires is bulk-addressable | `refuted` | `POST /import/source-notes` is a genuine batch route: `sourceIds` array capped at 100, `importConcurrency` 1–10, `extract` defaulting to true, returning a batch report with `batchStatus`, `imported[]`, `writeFailures[]`, `missingSourceIds[]`. `POST /notes/batch`, `POST /notes/permanent-delete` and `POST /notes/transfer` are also batch. The Sources UI has multi-select with select-all and posts `sourceIds` in bulk, abortable. **The narrowed claim that survives:** per-source re-extraction is single-item and serialized, and review accept/skip is per-draft. |
| Extraction has no batching endpoint, no progress stream, and no server-side job model | `partial` | No SSE, websocket, polling interval, job table or pollable operation status exists — `operationId` is minted per call and only echoed back, with no route to query it. But a batching endpoint **does** exist (`POST /import/source-notes`, up to 100 sources at concurrency up to 10). It is still one long synchronous request returning only a terminal report, so progress mid-batch is unobservable; the only mid-flight control is client-side abort, which the server does honor. |
| Re-running extraction silently skips work unless server-side fingerprints are cleared, and no interface does that | `refuted` on both halves | A stock re-extract control exists in the Sources UI (`reextract()` → `POST /notes/:id/extract`, surfaced as two buttons). And extraction never skips: `prepareLongTermMemorySource()` calls the model unconditionally; the fingerprint is written after the fact and is used only for a post-hoc 409 concurrency guard, draft staleness labelling, and preview freshness colouring — never as a skip gate. **The residual true observation:** re-extract is single-item and serialized, and the 409 guard can abort a re-extract *after* the model call has already been paid for if scope or modes changed concurrently. |

## Recall, ranking and injection

| Claim | Verdict | What is actually true | Where |
|---|---|---|---|
| Recency weighting is declared but unreachable | `confirmed`, with a naming caveat | `reciprocalRankFuse(lanes, options = {})` consumes `options.cooldowns` to set `cooldownPenalty` and scale `score`; the sole caller passes no options, so the branch is dead. The caveat: the type is a generic caller-supplied score multiplier, not an age-derived one — nothing in the code computes recency from anything. No other recency or age weighting exists: lanes are direct-metadata, BM25, keyword, graph and vector, and `chunk.updatedAt` is never read by ranking or budgeting. `usage.ts` writes `lastInjectedAt` and `retention.ts` reads it, but only for pruning decisions, never for scoring. | `ranking.ts` → `reciprocalRankFuse`, `LtmRankCooldown`; `retrieval.ts` → `retrieveLongTermMemory`; `usage.ts`; `retention.ts` |
| The absence of a date field is the chronology gap | `partial` — the reframing is correct | No in-world date or time field exists anywhere: notes, sections and chunks carry only wall-clock `createdAt`/`updatedAt`, and `timeline_event` is an ordinary note type whose story date can only live in prose. No timestamp of any kind is rendered into the prompt. Injected chunks are ordered `tier → score → chunk.id`, with `timeline_event` in tier 3, so within a `[TIMELINE]` block bullets appear in relevance order tie-broken by id string. **The real finding is the absence of chronological ordering or relation between event memories at injection**, which holds in every mode. | schema `ltmSectionSchema`/`ltmMemoryChunkSchema`; `prompt.ts` → `serializeLongTermMemoryPrompt`; `budget.ts` → `applyLtmBudget`, `tierFor` |
| Real-world time is not injected, so a date field would be pointless | `partial` | Engine behavior differs by mode. Conversation mode injects real time **by default** — a current-context block with time, date and weekday, plus `[DD.MM HH:MM]` prefixes on messages, and the default prompt tells the model to use them. Roleplay injects none by default (`{{date}}`/`{{time}}` are opt-in author macros). Game mode injects an in-fiction clock (`Day N, HH:MM`), not real time. So in the one mode where the engine does supply timestamps, LTM contributes nothing time-anchored to correlate with them. | `generate.routes.ts` conversation branch; `conversation-context-block.ts` → `buildConversationCurrentContextBlock`; `conversation-history-runtime.ts`; `macro-engine.ts` |
| Injection strips provenance the model could have used | `partial` — misframed, though a real loss exists | The stripper removes only two legacy patterns, a trailing `[note: …]` suffix and inline `[evidence: …]` spans, and **nothing in the current codebase writes either** — it is cleanup for legacy vault text, not an active provenance-destroying step. The substantive loss happens earlier: `chunkNoteSections` never copies a section's structured `evidence[]` onto the chunk, and the chunk schema has no evidence field, so quotes are unavailable to the injector by construction. Character and relationship chunks are grouped under a note title; `world`, `timeline_event`, `thread`, `tone` and `scene` are emitted as bare bullets under a category header with no title, id or section key. The user can trace a recalled note post hoc via the last-injection summary; the **model** cannot attribute a `[WORLD]` or `[TIMELINE]` bullet to anything. | `chunking.ts` → `cleanLongTermMemoryChunkText`, `chunkNoteSections`; `prompt.ts` → `memoryTitle`; `LastInjectionSummary.tsx` |
| Budget explain data never reaches the interface | `partial` | Per candidate the engine computes `score`, `normalizedScore`, `finalNormalizedScore`, `relevanceScore`, `reasons[]`, `lanes[]`, `laneScores{}`, `rawLaneScores{}`, `cooldownPenalty`, plus `tier`/`estimatedTokens` on selected and `rejectionReason` on rejected. Rejected candidates are only populated when `explain` is set, which comes from `recall.debugEnabled`. With debug on, one `recall_explanation` event is written carrying note id, section key, relevance score, lanes, reasons, estimated tokens, and rejection reason — and the stock activity view **does** render it, showing title · section · relevance % · lanes for selected and humanized rejection reasons for rejected, alongside the budget settings. **Never emitted anywhere at all:** `laneScores`, `rawLaneScores`, `normalizedScore`, `finalNormalizedScore`, the raw fused `score`, `tier`, and `cooldownPenalty`. With debug off, the only recall surface is note titles and token counts. | `ranking.ts` → `LtmRankedCandidate`; `budget.ts` → `LtmBudgetRejectedCandidate`; `generation-injection.ts` → `prepareGenerationLongTermMemory`; `ActivityView.tsx` |
| Memories are born ineligible for the chat mode that created them | `refuted` for the common path, `confirmed` for two narrower ones | Mode is never model-chosen: extraction compiles with a single `resolvedMode`, and in the normal chat path the extract route stamps `[ltmModeForChatMode(chat.mode)]`, so chat-derived memories **inherit the originating chat's mode correctly**. It breaks for **character and lorebook imports**, which are stamped all three modes with `extractionMode: "roleplay"` — extracting one with no `chatId` and no explicit mode resolves to roleplay-only regardless of context. It also breaks for **manual creation**, where the vault hardcodes `modes: ["roleplay"]`. This explains the first-run observation exactly: that walkthrough imported a character source. | `retrieval.ts` mode filter; `source-extraction.ts` → `resolvedMode`; `routes.ts` extract handler; `interop.ts` import stamping; `MemoryVault.tsx` → `newNote` |
| Mode eligibility is not shown or settable at accept time | `confirmed` | The review queue displays the source's modes as read-only text only. The sole editable control is a per-mode checkbox set inside the vault note editor, plus a bulk modes action — leave review, open the vault, find the note, open the editor, toggle. | `ReviewQueue.tsx`; `MemoryVault.tsx` editor and `batch("modes")` |
| "The conversation is not an ingestible source" duplicates the mode-eligibility finding | `refuted` — they are distinct | One is about what can enter the pipeline, the other about where output is visible. Verified independently: exactly three source kinds exist (`character`, `lorebook`, `chat_summary`), and the note-create schema excludes `source`, so source notes can only originate from the import routes — there is no pasted-text or manual source path. For chat sources, only `chat.metadata` is read: summary entries (skipping disabled ones), the legacy summary string, game session summaries, or conversation summaries. **Raw messages are never read.** Both the content and the timing of what LTM can learn from a chat are therefore controlled by the engine's separate auto-summary service. | schema `ltmSourceProvenanceSchema`, `createNoteBody`; `interop.ts` source branches |

## Auth failures and observability surfaces

| Claim | Verdict | What is actually true | Where |
|---|---|---|---|
| With no admin secret the settings screens fail and the UI never says why | `confirmed` | Every LTM route is registered as privileged with a blanket `onRequest` hook — `/status` is as gated as `DELETE /data`, with no per-route granularity. With the secret unset and the request not loopback-exempt, the gate returns 403 `"ADMIN_SECRET is required for privileged APIs"`. The client's `request()` reads only `payload.error` and never inspects the status, so 403 is indistinguishable from 500 downstream; the settings view then discards the message entirely and renders a fixed **"Could not load memory settings."** The same pattern produces "Could not load language connections.", "Integrity check could not load.", "Long-Term Memory status could not load." and "Could not load activity." A search for `403` or `ApiError` across the whole stock LTM client returns nothing. Write paths are better — a mutation does surface the server message. | `server-entry.ts` → `registerPrivilegedRoutes`; engine `capability-route-registration.service.ts` → `registerCapabilityPrivilegedRoutes`; engine `privileged-gate.ts` → `requirePrivilegedAccess`; `api.ts` → `request()`; `MemorySettings.tsx` |
| — aggravating detail | `confirmed` | The engine already ships `getPrivilegedActionErrorMessage` and `PRIVILEGED_ACCESS_HINT`, which turn a 403 into actionable copy naming the remedy. The package's private client does not use them, even though it sends `X-Admin-Secret` from the same localStorage key the shared admin-access box writes. The fix is wiring, not new copy. | engine `api-client.ts`; LTM `api.ts` |
| Loopback bypass | `confirmed` | `isLoopbackIp(request.ip) && !isAdminSecretRequiredOnLoopback()` passes before the secret is consulted; the override env var defaults to false. Because capability routes are registered without a trusted-network option, LAN and tunnel addresses get no bypass even though the engine supports one. | engine `privileged-gate.ts`; `runtime-config.ts` |
| No host-binding guidance | `refuted` as documentation, `confirmed` as runtime signal | The tradeoff is documented thoroughly across configuration, remote-access, troubleshooting and FAQ docs, and `.env.example` ships `HOST=0.0.0.0` with the relevant comment. But there is **no code-level validation or warning** — nothing logs or surfaces the security posture of the binding at startup. The only wildcard-host awareness in code is functional, used to decide origin trust. | engine docs; `index.ts` listen path; `runtime-config.ts` → `getHost`; `host-validation.ts`; `csrf-protection.ts` |
| Embedding host is hard-wired, rebuild reports success, zero count is a plain value | `confirmed`, all three | The capability embedding host is a frozen object with one hard-coded space id and a single local call — no remote provider, no fallback — while the engine's own recall path supports remote connections, dedicated embedding connections and a local sidecar. When the native binding is missing, the check returns false with a single info log, embedding returns null, and the rebuild still marks indexes clean and returns normally, so index health reads healthy. The status response does expose `embeddingsAvailable: false`, so the data exists — nothing raises on it. **No UI surface mentions embeddings at all**: a search for "embed" across the stock client and its locale file returns zero hits, and the detail view shows only total indexed chunks, which stays high while the semantic lane is dead. | `capability-embedding.service.ts`; `local-embedder.ts` → `hasNativeOnnxRuntimeBinding`; `rebuild.ts` → `markLtmIndexesClean`; `ltmStatusResponseSchema`; engine `memory-recall-embedding.ts` |
| Five observability endpoints exist and nothing assembles them | `refuted` — four of five are consumed | Rejected suggestions render in the review queue; the debug log powers a 200-event activity feed with filters, operation grouping, clear and export; last-injection renders fully in two places with per-note titles, token costs and click-through; integrity renders an ok/fail banner with note count and the first 20 issues. `/status` is the thin one: only total notes and indexed chunk count are used, while `indexes.errors`, `indexes.warnings`, `dirty`, `generatedAt`, `sourceHash`, `notes.byType`, `notes.byStatus`, `events` and both embedding fields are fetched and dropped. **The accurate claim:** they are assembled in four separate places, one of which is not a navigation destination at all — the activity view lives inside the settings debug tab behind a "Record debug activity" toggle that defaults off — and the diagnostic fields of `/status` are precisely the ones discarded. | `routes.ts`; `ReviewQueue.tsx`; `ActivityView.tsx`; `LastInjectionSummary.tsx`; `MemorySettings.tsx`; `LongTermMemoryDetail.tsx`; `LongTermMemoryNavigation.tsx` |
| There is no vault-state view at all | `refuted` | A health pill and a warning banner exist, shown when health is not healthy, with a popover: derived index health, indexed chunk count, and a pointer to Settings → Maintenance → Reindex. Omitted though fetched: note counts by type, embedded chunk counts, index errors and warnings, dirty flag, generation timestamp. Integrity problems live in a different component again. So a health view exists, split across two places, showing roughly two of six things. | `LongTermMemoryDetail.tsx` health pill and warning; `MemorySettings.tsx` maintenance |
| Source notes inflate vault counts with no way to exclude them | `confirmed` | `source` is a note type in the same store; the summary sums totals with no type exclusion, and that feeds the navigation badge. The vault queries notes with no type parameter and renders source notes first among the type groups, counted in the "shown" total. Filters available are status and free-text search only — no type filter, no source-exclusion toggle. The retrieval path *does* exclude them, and another endpoint's schema excludes `source` explicitly, so the vault total genuinely overstates retrievable memories with no way to reconcile the two numbers. | schema `ltmNoteTypeSchema`; `index-state.ts` → `writeLtmNoteSummary`; `MemoryVault.tsx` → `groupedNoteTypes` |

## Extraction mechanics

| Claim | Verdict | What is actually true | Where |
|---|---|---|---|
| An unset `personaId` on the extraction scope caused the untrusted-subject drops | `partial` — the mechanism named is wrong | The trusted persona roster is built from the **resolved chats'** persona ids, gathered via `scope.chatIds`/`groupId`. `scope.personaId` is never read when building the roster: setting it alone would not add the persona, and omitting it while `chatIds` is present still trusts the persona. It has only a second-order effect, by changing which existing notes enter the catalog. The load-bearing field is `chatIds`. In the stock UI, only the "current chat" import target carries `personaId` at all; every other target builds scope by hand without it, and the "All available" target passes no scope, producing an **empty roster** — at which point everything is untrusted. | `subject-identity.ts` → `loadTrustedLtmSubjectCatalog`, `subjectRejection`; `chat-scope.ts` → `resolveChatLtmScope`; `SourcesWorkspace.tsx` import targets; `MemoryVault.tsx` scope editor |
| No validation or refusal when the trusted roster is empty | `confirmed` | Enforcement is switched on whenever the catalog object exists, and it is always constructed — so a zero-entry roster still enforces. Nothing checks roster emptiness before spending the model call; the drops surface only afterward as per-candidate rejections. | `source-extraction.ts` `enforceTrustedSubjects`; `subject-identity.ts` |
| `personaId` is settable in the stock UI | `refuted` | The scope editor renders it only as a removable pill. The add controls are a chat/group select and a character-id input; there is no add-persona control anywhere in the feature. It is populated by the API or by chat-scope resolution only. | `MemoryVault.tsx` scope editor |
| The evidence-unit schema is a discriminated union | `refuted` | It is a flat object schema with `.strip()`. Bucket-specific rules are enforced separately, in prompt text and in the JSON-Schema `allOf`/`if`/`then` block. | schema `ltmEvidenceUnitSchema`; `evidence-unit-extraction.ts` → `evidenceUnitResponseFormat` |
| Explicit `null` for an optional object field is rejected, and the repair pass could normalize it | `confirmed`; bug-vs-intent is an inference | Optional fields are `.optional()` and never `.nullable()`, so a unit carrying `"dimensions": null` fails validation and is dropped whole as `invalid_format`. The repair pass rewrites only `id`, `sourceHash` and `links` — and it *does* coerce a non-array `links` to `[]`, demonstrating the code knows how to coerce junk. Nothing expresses an intent to reject nulls and there is no comment either way, so this reads as an unintended gap rather than deliberate strictness. | `evidence-unit-extraction.ts` → `normalizeEvidenceUnitResponse`, `normalizedEvidenceUnitRecord`, `parseEvidenceUnitPayload` |
| Structured-output requirements are model-specific | `refuted` | The requirement is generic OpenAI-compatible `json_schema` with `strict: false` — not strict mode, not tool calling. Strict mode is avoided deliberately because the schema uses `format`, `pattern`, length bounds, `uniqueItems` and conditional subschemas that strict mode rejects. There is also a graceful degradation path: provider errors matching "response format unsupported" trigger a retry with the response format removed, salvaging fenced or embedded JSON — which is precisely the regime where null-for-optional and shape drift bite hardest. | `evidence-unit-extraction.ts` → `evidenceUnitResponseFormat`, `isResponseFormatUnsupportedError`, `chatCompleteWithReasoningFallback`, `extractJsonObject` |
| undici's default 300s headers timeout kills long extraction requests | `refuted` as stated | The 300s is a deliberate engine default, `DEFAULT_CHAT_GENERATION_TIMEOUT_MS`, overridable via `CHAT_GENERATION_TIMEOUT_MS` between 10s and 1h — it merely coincides with undici's default. It applies to the **server's own outbound model call**, not the client hop: the LTM client sets no timeout and the app leaves Fastify's request timeout disabled. Extraction is non-streaming, so the headers timeout covers the whole wait for the first byte. The LTM package cannot set a timeout at all — no timeout field is exposed on the capability language-model surface. Separately, the per-request override helper exists and is never called anywhere: dead code. | engine `base-provider.ts` → `llmAgentOptions`, `withLlmRequestTimeout`; `runtime-config.ts` → `getChatGenerationTimeoutMs`; `package-runtime.ts` capability surface |
| Nothing guards an oversized source before spending a model call | `partial` | An input-side preflight throws `ltm_model_context_capacity` **before** any model call when the prompt would need trimming — but it early-returns unless the provider reports a known context window, so providers that don't get no guard. There is no byte-size limit on the source note and no chunking; the code states outright that source text is never truncated and tells the user to split manually. | `evidence-unit-extraction.ts` → `preflightExtractionPromptContext` |
| Output-limit truncation fails the extraction outright | `confirmed`, with waste worth noting | A finish reason of length/max-tokens throws `ltm_model_output_truncated` and the **entire response is discarded**, not partially salvaged, even though a valid JSON prefix exists. `maxOutputTokens` is stock-UI configurable, clamped 512–32,768 and floored by the model's own limit. | `evidence-unit-extraction.ts` → `runLongTermMemoryEvidenceUnitExtraction`; `MemorySettings.tsx` |
| An extraction that discards a third of its output reports as success | `partial` — the state is honest, the numbers are not | The outcome state is computed as `partial_success` whenever anything was kept and anything was dropped or deduplicated, and the UI does render a warning-toned "Partial success" badge. But the only completion summary counts requested / wrote / succeeded / failed / cancelled / missing / write failures — `totalCandidates`, `keptUnits` and `droppedUnits` appear nowhere in the import panel, and drop counts with per-candidate reasons live one navigation away in the review queue. | schema `ltmExtractionOutcomeSchema`; `evidence-unit-extraction.ts` → `summarizeExtractionOutcome`; `SourcesWorkspace.tsx` → `importResultSummary` |
| A partly-failed extraction leaves drafts permanently blocked behind a misleading "source changed" message | `refuted` — the causal chain is wrong | Only the first step holds: the fingerprint gate does refuse to stamp the source note on an LLM `partial_success`. But draft freshness compares the **draft's own** fingerprint against one recomputed from the source note's live content and never reads the note's stored fingerprint. So a partial extraction yields `fresh` with **zero block reasons**. The `hashless` state fires only for legacy drafts predating context-bound extraction. The missing note fingerprint surfaces instead as an accurately-named "Extraction incomplete" label with warning tone. The "source or extraction context changed" wording exists but belongs to a different trigger entirely. | `source-processing.ts` → `canMarkCurrent`; `draft-review.ts` → `draftFreshness`; `source-hash.ts` → `isLtmSourceExtractionFingerprintCurrent`; `interop.ts` → `previewFreshness` |
| Freshness is recomputed at read time and a draft can silently become blocked | `confirmed` on mechanism and silence; the trigger needs correcting | Freshness is derived on every review read and never stored. It flips when any fingerprint input drifts: the source section's text or evidence, the note title or id, provenance, modes, extraction mode, or normalized scope — which covers chat ids, group and character ids but **deliberately excludes `personaId`**. Editing a character card does **not** directly invalidate anything; it invalidates only once a re-import rewrites the source note's own text. Adding a chat to scope, renaming the note, or changing modes does it immediately. No event, notification or log entry is emitted on the transition. What the user sees: the freshness pill changes from Fresh to Stale, a red blocks panel appears reading "Source Stale: The source or extraction context changed after this extraction.", the draft is excluded from bulk apply by a freshness filter, the blocked count increments, and applying anyway returns 409. So the *block* is clearly labeled once the queue is open; what is silent is the transition and any prompt to go look. | `source-hash.ts` → `extractionFingerprintForLtmSourceMaterial`, `normalizedScope`; `draft-review.ts` → `blockReasonsForDraft`; `ReviewQueue.tsx` → `freshnessLabel`, blocks panel |

## Engine — character memories and summary ordering

| Claim | Verdict | What is actually true | Where |
|---|---|---|---|
| Concluded scenes write global `characterMemories[]` onto the character card | `confirmed` | Concluding a scene pushes an entry for every character in the scene, shaped `{ from: personaName, fromCharId: "scene", summary: "[Scene on <date>] <summary>" }`. There is also a second write path, the documented `[memory: target=…, summary=…]` command, whose target is resolved by fuzzy name match **across the entire character list** rather than the chat's participants. | `scene.routes.ts` conclude handler; `conversation-side-effect-command-runtime.ts` → `handleMemoryCommand`; `character-commands.ts` → `MEMORY_RE` |
| It leaks scene-local state into every chat using that card | `partial` — true for a day, not permanently | Injection is filtered to same-day entries only: `mergeConversationCharacterMemories` keeps memories whose `createdAt` is at or after the start of the local day. The entries persist on the card forever but stop entering prompt context at the next local midnight. A second reader, schedule-continuity regeneration, uses a different window — memories since the schedule's week start, top 8. Both are card-global rather than chat-scoped, so any conversation containing that character picks them up within those windows. | `conversation-memory-context.ts` → `mergeConversationCharacterMemories`, `getZonedDayBounds`; `conversation.routes.ts` → `buildScheduleContinuityContext` |
| It is untyped and unmanageable | `confirmed` | `characterMemories` is not a typed field: card `extensions` is `Record<string, unknown>`, so this is an unvalidated stowaway key, locally re-declared in three separate modules. No client code references it anywhere — the "Memories" modal in chat settings manages a different, chat-scoped feature. There is no view, edit or clear affordance and no endpoint touching the array; the only removal path is editing card extensions wholesale. Writes are deliberately snapshot-suppressed, so there is no version history either. | `character.ts` → `CharacterExtensions`; `character.schema.ts` → `characterExtensionsSchema`; three local re-declarations |
| The cross-chat scope is intended behavior | `partial` | The `[memory:]` command is a documented feature — the Memories toggle is described as letting characters create memories for other characters — and scene docs note that *converting* a scene writes no character memory, implying concluding one does. The **cross-chat scope is documented nowhere**, so the leakage is an undocumented consequence of a card-global store rather than a designed guarantee. | `docs/conversation/getting-started.md`, `docs/conversation/calls.md`, `docs/roleplay/scenes.md` |
| Summary ordering derives from a sequence number rather than narrative position, inverting event order within a day | `refuted` | There is **no `seq` field and no date field** on a summary entry — it carries `rangeStartIndex`, `rangeEndIndex`, `messageCount`, `messageIds`, `createdAt`, `updatedAt`. `sortChatSummaryEntries` sorts primarily on `rangeStartIndex`, so **narrative position is already the primary key**, with `createdAt` and then original array index only as tie-breaks; both compilation and normalization re-sort. No within-a-day collision mechanism exists either: conversation summaries are a map keyed one entry per day, so a shared date is structurally impossible. | `chat.ts` → `ChatSummaryEntry`; `chat-summary-entries.ts` → `sortChatSummaryEntries`, `compileChatSummaryEntries`, `normalizeChatSummaryEntries` |
| — the real weakness, which is the inverse | `confirmed` | Entries *lacking* `rangeStartIndex` — every automated entry and most manual ones, i.e. `sourceMode` of `last` or `agent` — collapse to `Number.MAX_SAFE_INTEGER` and are therefore ordered by creation time rather than narrative position. `messageCount` and `messageIds` exist on those entries and could pin position; neither is consulted by the sort. Separately, un-summarized day details are emitted in raw object-key order, an unrelated ordering soft spot. | `chat-summary-entries.ts` → `sortChatSummaryEntries`; `conversation-history-runtime.ts` → `collectConversationKeyDetails` |
| Mis-ordering propagates into the memory timeline | `refuted` | LTM reads `summaryEntries` raw and unsorted, relying on the persisted order the engine already sorted, and re-sorts conversation summaries by date. Each entry becomes an independent source note carrying `message_range:<range>` evidence, candidate order is overridden by the user's selection order, extraction is per-source, and timeline order derives from `timeline_event` note content and links. **Source array position has no load-bearing effect on the resulting timeline.** | LTM `interop.ts` → `summaries()`, `conversationSummaryEntries`; `evidence-unit-extraction.ts`; `evidence-unit-validation.ts` |

## Open — under investigation

Nothing outstanding. All claims raised so far carry a verdict above.

Rows land here when a claim is raised and clear out when a verdict is recorded.
Currently pending: stock review UI capabilities (grouping, bulk actions, filters,
decision persistence, keyboard navigation, edit-before-accept discoverability,
conflict options, cap indicators, rejection display); attribution of the
20,000-char error message, the dedup implementation, the HTML review export and
keyword pruning between stock package and operator tooling; whether `personaId` is
reachable through the stock UI; structured-output requirements and whether
null-for-optional rejection is a bug; which hop the 300s timeout belongs to;
whether the real chronology finding is ordering between event memories rather than
a missing date field; the actual impact of provenance stripping; the ADMIN_SECRET
403 path and what the UI says when it fails; and a per-endpoint account of which
observability data the stock UI already surfaces.
